---
interface PricingRow {
  name: string;
  details: string;
  price: string;
}

interface Props {
  title?: string;
  columns?: { name: string; details: string; price: string };
  rows: PricingRow[];
}

const {
  title = "Tarifas",
  columns = { name: "Nivel", details: "Detalles", price: "Precio" },
  rows,
} = Astro.props;
---

<section class="py-24 bg-white">
  <div class="max-w-7xl mx-auto px-4">
    <div class="mb-12">
      <h2 class="text-4xl font-bold uppercase">{title}</h2>
      <div class="w-20 h-1.5 bg-primary mt-4"></div>
    </div>
    <div class="scroll-animate overflow-x-auto" data-animation="fadeInUp">
      <table class="w-full text-left border-collapse">
        <thead>
          <tr class="bg-forest-dark text-white">
            <th
              class="p-6 text-lg uppercase tracking-wider font-bold rounded-tl-xl"
              >{columns.name}</th
            >
            <th class="p-6 text-lg uppercase tracking-wider font-bold"
              >{columns.details}</th
            >
            <th
              class="p-6 text-lg uppercase tracking-wider font-bold rounded-tr-xl text-right"
              >{columns.price}</th
            >
          </tr>
        </thead>
        <tbody>
          {
            rows.map((row, i) => (
              <tr
                class:list={[
                  "group cursor-default border-b border-gray-200 hover:bg-primary/15 hover:border-l-4 hover:border-l-primary transition-all duration-300",
                  i % 2 === 1 && "bg-primary/5",
                ]}
              >
                <td class="p-6 font-bold text-xl group-hover:text-primary transition-colors duration-300">{row.name}</td>
                <td class="p-6 text-gray-500 italic">{row.details}</td>
                <td class="p-6 text-right font-bold text-2xl text-primary">
                  <span class="inline-block group-hover:scale-110 transition-transform duration-300">{row.price}</span>
                </td>
              </tr>
            ))
          }
        </tbody>
      </table>
    </div>
  </div>
</section>
